# Dev/test-only dependencies (not installed in production).
# Run tests:  python -m pytest tests/ -q
# Run benchmark: python -m benchmark.runner
pytest>=8.0
httpx>=0.27
pyyaml>=6.0
psycopg[binary]>=3.2
